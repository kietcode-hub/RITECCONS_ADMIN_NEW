node_modules/
dist/
build/
.env
.env.local
*.log
coverage/
.DS_Store
apps/api/generated/

# Locally-installed Claude Code skills (e.g. `uipro init`) - dev tooling, not project source.
# Keep CLAUDE.md itself tracked; this only excludes .claude/.
.claude/
